# Core (needed for train / evaluate / Lambda)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
matplotlib>=3.7          # confusion matrix PNG; optional, guarded at import

# AWS (Lambda runtime already ships boto3; needed locally for deploy/analytics)
boto3>=1.34

# Optional embedding backends
# sentence-transformers>=2.2   # --backend sbert
# torch>=2.0                   # required by sentence-transformers
